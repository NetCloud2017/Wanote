# runtime 运行时选择
runtime 的默认值是 'nodejs', 只有两个值： 'nodejs' 和 'edge';

    可以为单个路由指定运行时， 在page.js中设置： 



```js
// app/page.js
export const runtime = 'edge'

```
在布局 layout.js 中设置时， 所有用到该布局的路由都会使用该运行时。 


```js
// app/layout.js
export const runtime = 'edge'

```
